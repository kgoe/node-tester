/*
if ( typeof global !== 'undefined' ) {
  var prop = Object.getOwnPropertyNames(global);
  var str = JSON.stringify(prop,'','\n');
  prop.map(function(item){
      console.log("'" + item + "',");
  });
  //console.log(str);
}

if ( typeof window !== 'undefined' ) {
  var prop = Object.getOwnPropertyNames(window);
  var str = JSON.stringify(prop,'','\n');
  prop.map(function(item){
      console.log("'" + item + "',");
  });
  //console.log(str);
}
//*/


